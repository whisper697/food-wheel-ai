# .env.example  —  AI 密钥脱敏模板
# ================================================================
# ⚠️  本文件是安全占位模板，可以提交到 GitHub。
# ⚠️  请将本文件复制为 .env，并填写你的真实密钥，但绝对不要提交 .env！
# ================================================================
#
# 使用方法：
#   cp .env.example .env      # 复制模板
#   然后打开 .env，把下方占位值替换为真实密钥
#
# ─────────────────────────────────────────────────────────────────
# OpenAI API 密钥（必填）
# 获取地址：https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Base URL（可选，使用第三方中转服务时填写）
# 若直接使用 OpenAI 官方，删除此行或保留注释即可
# OPENAI_BASE_URL=https://api.openai.com/v1

# AI 模型名称（可选，默认使用 gpt-3.5-turbo）
# 可选值示例：gpt-3.5-turbo / gpt-4o / gpt-4-turbo
# AI_MODEL=gpt-3.5-turbo
